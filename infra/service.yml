apiVersion: v1
kind: Service
metadata:
  name: chess-deployment-service
spec:
  type: LoadBalancer
  selector:
    app: chess-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
